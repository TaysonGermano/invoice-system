import { createUseStyles } from "react-jss";

export default function Theme() {
  const light = <i class="fas fa-sun"></i>;
  const dark = <i class="fas fa-moon"></i>;

  return <div>{light}</div>;
}
